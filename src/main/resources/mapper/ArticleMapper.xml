<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.article.mapper.ArticleMapper">

    <update id="incrementLikeCount">
        UPDATE article
        SET like_count = like_count + #{delta}
        WHERE id = #{articleId}
    </update>

    <update id="incrementFavoriteCount">
        UPDATE article
        SET favorite_count = favorite_count + #{delta}
        WHERE id = #{articleId}
    </update>

    <update id="incrementCommentCount">
        UPDATE article
        SET comment_count = comment_count + #{delta}
        WHERE id = #{articleId}
    </update>

    <update id="incrementViewCount">
        UPDATE article
        SET view_count = view_count + #{delta}
        WHERE id = #{articleId}
    </update>

</mapper>
